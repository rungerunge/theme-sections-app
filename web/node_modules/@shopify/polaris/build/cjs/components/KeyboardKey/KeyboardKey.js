'use strict';

var React = require('react');
var css = require('../../utilities/css.js');
var KeyboardKey$1 = require('./KeyboardKey.scss.js');
var hooks = require('../../utilities/features/hooks.js');

function KeyboardKey({
  children = '',
  size
}) {
  const {
    polarisSummerEditions2023
  } = hooks.useFeatures();
  const key = !polarisSummerEditions2023 && !size && children.length > 1 ? children.toLowerCase() : children.toUpperCase();
  const className = css.classNames(KeyboardKey$1.default.KeyboardKey, size && KeyboardKey$1.default[size], polarisSummerEditions2023 && KeyboardKey$1.default.small);
  return /*#__PURE__*/React.createElement("kbd", {
    className: className
  }, key);
}

exports.KeyboardKey = KeyboardKey;
