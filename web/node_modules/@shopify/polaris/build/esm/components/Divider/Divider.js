import React from 'react';
import styles from './Divider.scss.js';
import { useFeatures } from '../../utilities/features/hooks.js';

const Divider = ({
  borderColor = 'border-subdued',
  borderWidth = '1'
}) => {
  const {
    polarisSummerEditions2023
  } = useFeatures();
  const borderColorValue = borderColor === 'transparent' ? borderColor : `var(--p-color-${polarisSummerEditions2023 && borderColor === 'border-subdued' ? 'border-faint-experimental' : borderColor})`;
  return /*#__PURE__*/React.createElement("hr", {
    className: styles.Divider,
    style: {
      borderBlockStart: `var(--p-border-width-${borderWidth}) solid ${borderColorValue}`
    }
  });
};

export { Divider };
